#!/bin/bash

EXECUTABLE="./client -h 143.248.56.16 -p 3000 -o 0 -s 3"
FILENUM=$(\ls -afq | grep .in | wc -l)

rm -f *.out

echo "==============================================================="
echo "TEST $1"
echo "---------------------------------------------------------------"
.././$EXECUTABLE < $1.input > $1.out
if ! diff -q $1.out $1.correct > /dev/null  2>&1; then
    echo "---------------------------------------------------------------"
    echo "$1 FAILED"
    echo "---------------------------------------------------------------"
#        echo "$(diff $i.out $i.correct)"
    echo "==============================================================="
else
    echo "---------------------------------------------------------------"
    echo "$1 PASSED"
    echo "==============================================================="
fi
